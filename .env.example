# Copy to .env for local dev (docker compose reads its own values from compose.yaml).

# Postgres connection. Local dev via the throwaway container:
DATABASE_URL=postgres://postgres:pb@localhost:5544/plan_builder

# Where rendered plan tiles / PDFs / glyph crops are written.
STORAGE_DIR=./storage
